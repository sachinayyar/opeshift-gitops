apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
  labels:
    app: sample-spring-boot
    app.kubernetes.io/component: web
    app.kubernetes.io/instance: sample-spring-boot
    app.kubernetes.io/name: sample-spring-boot
    app.kubernetes.io/part-of: sample-spring-boot
    app.openshift.io/runtime: java
  name: sample-spring-boot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sample-spring-boot
  template:
    metadata:
      labels:
        app: sample-spring-boot
    spec:
      containers:
      - name: sample-spring-boot
        imagePullPolicy: Always
        image: quay.io/sachinayyar/tekton-argocd:1
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 45
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        ports:
        - containerPort: 8080
          protocol: TCP
        - containerPort: 8443
          protocol: TCP
        - containerPort: 8778
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 45
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 5

